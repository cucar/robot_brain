[package]
name = "brain-core"
version = "0.1.0"
edition = "2021"
description = "Core brain computation engine — neurons, patterns, and hierarchical prediction"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rustc-hash = "2"
rayon = "1"

[dev-dependencies]
approx = "0.5"
